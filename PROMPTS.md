# 🤖 Prompts

Add your ideas below as bullet points. The AI Autopilot picks one every 4 hours,
generates code, opens a PR, reviews it, and merges if score >= 8/10.

- Build a multi-turn conversational AI chatbot with memory using LangChain
- Create a document Q&A app using RAG with vector embeddings
- Add an AI image caption generator using vision models
- Build a code explainer that takes any function and explains it in plain English
- Create a semantic search engine over personal notes
